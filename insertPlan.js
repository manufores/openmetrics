// const plan = document.getElementById("package").innerHTML;
const plan = "";
const info = "Gracias por interesarte en nuestro plan: "
console.log(plan);

function insertText(plan) {
    document.getElementById("textoform").value = info + plan;
}
